DELIMITER //

CREATE FUNCTION calculate_age(birthdate DATE)
RETURNS INT
DETERMINISTIC
BEGIN
RETURN TIMESTAMPDIFF(YEAR, birthdate, CURDATE());
END //

DELIMITER //

SELECT calculate_age('2020-01-01');

CREATE FUNCTION greet_user(name VARCHAR(100))
RETURNS VARCHAR(255)
DETERMINISTIC
BEGIN
    RETURN CONCAT('Hello, ', name, '!');
END //

DELIMITER ;

select greet_user('Ruslan');


# 1.



DELIMITER //
CREATE FUNCTION UPPER_CASE(X VARCHAR(100))
RETURNS VARCHAR(100)
DETERMINISTIC
BEGIN
    RETURN UPPER(X);
END //
DELIMITER ;

SELECT UPPER_CASE('ivan');

DELIMITER //

CREATE FUNCTION FUNC1(X INT)
RETURNS BOOLEAN
BEGIN 
	RETURN X % 2 = 0;
END //

DELIMITER ;

SELECT FUNC1(2.2);






